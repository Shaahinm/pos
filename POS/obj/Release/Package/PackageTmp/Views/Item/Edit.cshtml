@model POS.Models.EditViewModel

@{
    ViewBag.Title = "Edit";
}


<div class="ui top attached fluid tabular menu">
    <a class="item" data-tab="general">اطلاعات عمومی</a>
    <a class="active item" data-tab="additional">اطلاعات اضافه</a>
    <a class="item" data-tab="variant">متغییرات</a>
</div>
<div class="ui bottom attached tab segment" data-tab="general">
    <form id="CreateItemForm" class="ui form">
        @Html.Partial("Edit_partials/_General", @Model)
    </form>
</div>
<div class="ui bottom attached active tab segment" data-tab="additional">
    @Html.Partial("Edit_partials/_additional")
</div>
<div class="ui bottom attached tab segment" data-tab="variant">
    @Html.Partial("Edit_partials/_variant")
</div>
<div class="row">
    <div class="sixteen wide column">
        <div id="btnSave" class="ui primary submit button">Submit</div>
    </div>
</div>
